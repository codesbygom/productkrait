services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      REDIS_URL: redis://redis:6379/1
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    restart: unless-stopped
